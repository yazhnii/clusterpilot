apiVersion: v1
kind: Service
metadata:
  name: clusterpilot-service
spec:
  type: NodePort
  selector:
    app: clusterpilot
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
      nodePort: 30007